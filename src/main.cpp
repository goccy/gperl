#include <gperl.hpp>

int main(int argc, char **argv)
{
	GPerl gperl(argc, argv);
	//fprintf(stderr, "malloc_time = [%f]\n", malloc_time);
	//fprintf(stderr, "total_gc_time = [%f]\n", total_time);
	//gperl.setGC();
	//gperl.main();
	//gperl.close();
	return 0;
}
